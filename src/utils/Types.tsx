
export type FormFields = {
    label: string;
    valueType: string;
    required: boolean;
}

export type credentials = {
    email: string;
    password: string;
}

export type artist = {
    name: string;
}


export type track ={
    title: string;
    artist: string;
}